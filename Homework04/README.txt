This program does a 360 degree rotation finding the point with the most ir light and then rotates back to face it. Then, depending on if it was passed an argument of a zero or a one it will either follow the strongest light source of flee from it. 

call: ./IRTracker 0/1
